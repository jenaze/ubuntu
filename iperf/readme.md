
# 🌐 NoDelay iperf3 Connectivity Tester

یک ابزار کمکی و خودکار مبتنی بر پایتون برای تست کیفیت، پایداری و سرعت ارتباط مستقیم بین دو سرور (مانند سرور ایران و خارج) با استفاده از `iperf3`.
این اسکریپت به شما کمک می‌کند تا پیش از راه‌اندازی هرگونه تانل (Tunnel)، از مناسب بودن وضعیت شبکه بین دو سرور اطمینان حاصل کنید.

## ✨ ویژگی‌ها (Features)

- **نصب خودکار iperf3:** در صورت عدم وجود `iperf3`، اسکریپت به صورت خودکار آن را از طریق پکیج‌منیجر سیستم‌عامل (apt, dnf, yum, apk, pacman, zypper) نصب می‌کند.
- **منوی تعاملی (Interactive UI):** دارای رابط کاربری متنی زیبا و ساده برای استفاده آسان بدون نیاز به حفظ کردن دستورات.
- **پشتیبانی کامل از CLI:** امکان اجرای تست‌ها به صورت مستقیم از طریق آرگومان‌های خط فرمان (مناسب برای اسکریپت‌نویسی و اتوماسیون).
- **تست چندپورتی (Multi-port):** قابلیت اجرای همزمان `iperf3` روی ده‌ها پورت و رتبه‌بندی بهترین پورت‌ها بر اساس سرعت دانلود، آپلود و پایداری.
- **تحلیل کیفیت شبکه:** محاسبه میزان Retransmitها و نمایش وضعیت شبکه (Excellent, Good, Moderate, Poor).

---

## 🚀 شروع سریع (Quick Start)

برای اجرای سریع ابزار، کافیست آن را دانلود کرده و با پایتون اجرا کنید. 

**۱. دانلود اسکریپت:**
```
wget -O iperf3_tester.py https://raw.githubusercontent.com/jenaze/ubuntu/refs/heads/main/iperf/iperf3_tester.py
chmod +x iperf3_tester.py
```

*(توجه: آدرس لینک بالا را با لینک واقعی مخزن خود جایگزین کنید)*

**۲. اجرای منوی تعاملی:**

```bash
python3 iperf3_tester.py

```

با اجرای این دستور، منوی گرافیکی متنی باز می‌شود و می‌توانید به راحتی نقش سرور یا کلاینت را انتخاب کنید.

---

## 💻 نحوه استفاده از خط فرمان (CLI Usage)

این اسکریپت را می‌توانید با پاس دادن پارامترهای مختلف به صورت مستقیم اجرا کنید.

### راهنمای دستورات

برای دیدن لیست تمام آرگومان‌ها:

```bash
python3 iperf3_tester.py --help
```

### ۱. تست تک‌پورت (Single-port Test)

**روی سرور مقصد (مثلاً ایران):**

```bash
python3 iperf3_tester.py --mode server --port 9777

```

**روی سرور مبدا (مثلاً خارج):**

```bash
python3 iperf3_tester.py --mode client --host <SERVER_IP> --port 9777 --duration 10 --streams 8

```

* `<SERVER_IP>`: آی‌پی سروری که مرحله قبل روی آن اجرا شده است.
* `--duration`: زمان تست به ثانیه (پیش‌فرض: 8)
* `--streams`: تعداد کانکشن‌های موازی (پیش‌فرض: 8)

### ۲. تست چندپورتی (Multi-port Test)

این حالت برای پیدا کردن بهترین پورت‌های باز و بدون اختلال بین دو سرور بسیار کاربردی است.

**روی سرور مقصد:**
(اسکریپت به صورت خودکار روی پورت‌های ضروری و تعداد زیادی پورت رندوم لیسن می‌کند)

```bash
python3 iperf3_tester.py --mode server --multi

```

**روی سرور مبدا:**
(پورت‌هایی که می‌خواهید تست شوند را با کاما جدا کنید)

```bash
python3 iperf3_tester.py --mode client --host <SERVER_IP> --multi --ports 80,443,2053,9999 --duration 8

```

---

## 🛠 پیش‌نیازها (Requirements)

* **Python 3.6+** (بدون نیاز به نصب ماژول‌های جانبی، فقط از کتابخانه‌های استاندارد پایتون استفاده شده است).
* دسترسی روت (`root`) در صورت نیاز به نصب خودکار پکیج `iperf3`.

## 📝 لایسنس (License)

این پروژه تحت لایسنس [MIT](https://www.google.com/search?q=LICENSE) منتشر شده است. استفاده و تغییر آن برای همه آزاد است.
